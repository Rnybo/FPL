services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - fpl_data:/srv/data
    restart: unless-stopped

volumes:
  fpl_data:
    driver: local
